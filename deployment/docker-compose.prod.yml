version: "3.9"
services:
  bot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    restart: always
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
